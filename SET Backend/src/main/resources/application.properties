# ========================
# Server Config
# ========================
server.port=8080

# ========================
# Database Config
# ========================
spring.datasource.url=jdbc:mysql://localhost:3306/expense_tracker_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=2078955@iP
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========================
# JPA / Hibernate Config
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# ========================
# JWT Config
# ========================
app.jwt.secret=SmartExpenseTrackerSecretKey2024_ThisMustBeAtLeast256BitsLong!
app.jwt.expiration-ms=86400000

# ========================
# CORS Config
# ========================
app.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:5500,http://localhost:5500

# ========================
# Logging
# ========================
logging.level.com.expensetracker=DEBUG
logging.level.org.springframework.security=INFO
